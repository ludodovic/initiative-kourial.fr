services:
  initiative-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "127.0.0.1:${FRONTEND_PORT:-4200}:80"
    volumes:
      - initiative-uploads:/srv/initiative-uploads:ro

volumes:
  initiative-uploads:
    external: true
    name: ${UPLOADS_VOLUME:-initiative-api-prod_initiative_uploads}
